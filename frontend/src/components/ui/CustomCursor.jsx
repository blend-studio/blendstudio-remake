import React, { useEffect, useState } from "react";
import { motion, useSpring } from "framer-motion";

const CustomCursor = () => {
  return null; // Disabilitato per eliminare il carico di repaint dovuto ai blur mobili
};

export default CustomCursor;
